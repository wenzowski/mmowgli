This directroy holds some tarballs and zip files that 
are used during the install. Vagrant mounts this directory
as /vagrant on the VMs it brings up; the various VMs can
unpack the tarballs from there to provision the VM.

This directory typcially holds the JDK tar file, tomcat
tar file, activemq tar file, et.
